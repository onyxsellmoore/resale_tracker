name: Deploy — Push to Main

on:
  push:
    branches: [main]

concurrency:
  group: deploy-main
  cancel-in-progress: false

permissions:
  contents: read
  id-token: write

env:
  GCP_PROJECT: resales-tracker
  GCP_REGION: us-central1
  AR_REPO: us-central1-docker.pkg.dev/resales-tracker/booking
  SERVICE_NAME: booking-api
  WIF_PROVIDER: projects/resales-tracker/locations/global/workloadIdentityPools/github-pool/providers/github-provider
  WIF_SA: github-deploy@resales-tracker.iam.gserviceaccount.com

jobs:
  test-backend:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Run backend tests
        working-directory: booking-api
        run: ./mvnw -B verify

  test-frontend:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: booking-ui/package-lock.json

      - name: Install and test
        working-directory: booking-ui
        run: |
          npm ci
          npx tsc -b --noEmit
          npm test -- --run

  security-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: booking-ui/package-lock.json

      - name: OWASP Dependency Check
        working-directory: booking-api
        run: ./mvnw -B org.owasp:dependency-check-maven:check -DfailBuildOnCVSS=7 -DsuppressionFiles=../.github/owasp-suppressions.xml
        continue-on-error: true

      - name: npm audit
        working-directory: booking-ui
        run: |
          npm ci
          npm audit --audit-level=high || true

      - name: Trivy filesystem scan
        uses: aquasecurity/trivy-action@master
        with:
          scan-type: fs
          scan-ref: .
          severity: HIGH,CRITICAL
          exit-code: "0"

  deploy-backend:
    needs: [test-backend, test-frontend, security-scan]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.WIF_SA }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${{ env.GCP_REGION }}-docker.pkg.dev --quiet

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "25"
          cache: maven

      - name: Build JAR
        working-directory: booking-api
        run: ./mvnw -B package -DskipTests

      - name: Build and push Docker image
        working-directory: booking-api
        run: |
          IMAGE=${{ env.AR_REPO }}/${{ env.SERVICE_NAME }}:${{ github.sha }}
          docker buildx build --platform linux/amd64 \
            -f src/main/docker/Dockerfile.jvm \
            -t $IMAGE -t ${{ env.AR_REPO }}/${{ env.SERVICE_NAME }}:latest .
          docker push $IMAGE
          docker push ${{ env.AR_REPO }}/${{ env.SERVICE_NAME }}:latest

      - name: Deploy to Cloud Run
        run: |
          gcloud run deploy ${{ env.SERVICE_NAME }} \
            --image ${{ env.AR_REPO }}/${{ env.SERVICE_NAME }}:${{ github.sha }} \
            --region ${{ env.GCP_REGION }} \
            --project ${{ env.GCP_PROJECT }} \
            --set-secrets=QUARKUS_MONGODB_CONNECTION_STRING=mongo-uri:latest,BOOKING_JWT_SECRET=jwt-secret:latest,BOOKING_WEBAUTHN_RP_ID=webauthn-rp-id:latest,BOOKING_WEBAUTHN_ORIGIN=webauthn-origin:latest,CORS_ORIGINS=cors-origins:latest \
            --allow-unauthenticated \
            --min-instances=0 \
            --max-instances=2 \
            --memory=512Mi \
            --cpu=1 \
            --port=8080 \
            --quiet

  deploy-frontend:
    needs: [test-backend, test-frontend, security-scan]
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: booking-ui/package-lock.json

      - name: Install and build
        working-directory: booking-ui
        run: |
          npm ci
          cp .env.production.example .env.production
          npm run build
        env:
          VITE_API_URL: https://booking-api-<hash>-uc.a.run.app

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
          channelId: live
          projectId: ${{ env.GCP_PROJECT }}
          entryPoint: .
